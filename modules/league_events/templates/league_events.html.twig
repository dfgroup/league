{#
/**
 * @file league_events.html.twig
 * Default theme implementation to present League events data.
 *
 * This template is used when viewing League events pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_league_events()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('league_events') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
